# Secrets — never commit these
.env
.env.*
*.pem
*.key
credentials
config

# AWS CLI / SDK local config
.aws/

# Terraform (if used later)
*.tfstate
*.tfstate.*
.terraform/

# OS / editor
.DS_Store
.vscode/
__pycache__/
*.pyc
node_modules/

# Backend deploy secrets (live only on the EC2 instance)
infra/backend-deploy/.env
!.env.example
!infra/backend-deploy/.env.example
